version: "3.8"

services:
  api:
    build: .
    ports:
      - "8000:8000"
    depends_on:
      - redis

  worker:
    build: .
    command: python worker.py
    depends_on:
      - redis

  redis:
    image: redis:7-slim